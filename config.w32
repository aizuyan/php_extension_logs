// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("mylogs", "for mylogs support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("mylogs", "enable mylogs support", "no");

if (PHP_MYLOGS != "no") {
	EXTENSION("mylogs", "mylogs.c");
}

